import { HealWeaponPipe } from './heal-weapon.pipe';

describe('HealWeaponPipe', () => {
  it('create an instance', () => {
    const pipe = new HealWeaponPipe();
    expect(pipe).toBeTruthy();
  });
});
