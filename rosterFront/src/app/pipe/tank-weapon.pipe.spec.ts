import { TankWeaponPipe } from './tank-weapon.pipe';

describe('TankWeaponPipe', () => {
  it('create an instance', () => {
    const pipe = new TankWeaponPipe();
    expect(pipe).toBeTruthy();
  });
});
