import { InstanceFourPipe } from './instance-four.pipe';

describe('InstanceFourPipe', () => {
  it('create an instance', () => {
    const pipe = new InstanceFourPipe();
    expect(pipe).toBeTruthy();
  });
});
