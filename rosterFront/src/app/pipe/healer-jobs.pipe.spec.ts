import { HealerJobsPipe } from './healer-jobs.pipe';

describe('HealerJobsPipe', () => {
  it('create an instance', () => {
    const pipe = new HealerJobsPipe();
    expect(pipe).toBeTruthy();
  });
});
