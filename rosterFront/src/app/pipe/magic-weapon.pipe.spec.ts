import { MagicWeaponPipe } from './magic-weapon.pipe';

describe('MagicWeaponPipe', () => {
  it('create an instance', () => {
    const pipe = new MagicWeaponPipe();
    expect(pipe).toBeTruthy();
  });
});
