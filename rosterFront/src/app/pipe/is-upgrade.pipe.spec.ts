import { IsUpgradePipe } from './is-upgrade.pipe';

describe('IsUpgradePipe', () => {
  it('create an instance', () => {
    const pipe = new IsUpgradePipe();
    expect(pipe).toBeTruthy();
  });
});
