import { MeleeWeaponPipe } from './melee-weapon.pipe';

describe('MeleeWeaponPipe', () => {
  it('create an instance', () => {
    const pipe = new MeleeWeaponPipe();
    expect(pipe).toBeTruthy();
  });
});
