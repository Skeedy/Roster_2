import { RangedWeaponPipe } from './ranged-weapon.pipe';

describe('RangedWeaponPipe', () => {
  it('create an instance', () => {
    const pipe = new RangedWeaponPipe();
    expect(pipe).toBeTruthy();
  });
});
